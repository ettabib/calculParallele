
A <- function(u){
    return (-1000*cos(u))
}
a <- function(u){
    return (1000*sin(u))
}
phi <- function(u){
    return (a(u) ^ 2 / 0.2e1 - cos(u) / 0.2e1)
}
min.phi=-1/2*1000

Max.phi <- function(m){
  return (1*1000)
}
